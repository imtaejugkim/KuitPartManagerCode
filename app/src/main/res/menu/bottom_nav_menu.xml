<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/homeFragment"
        android:icon="@drawable/selector_home"
        android:state_checked="true"
        tools:ignore="MenuTitle"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/searchFragment"
        android:icon="@drawable/selector_search"
        tools:ignore="MenuTitle"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/posthFragment"
        android:icon="@drawable/selector_post"
        tools:ignore="MenuTitle"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/reelsFragment"
        android:icon="@drawable/selector_reels"
        tools:ignore="MenuTitle"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/profileFragment"
        android:icon="@drawable/selector_profile"
        tools:ignore="MenuTitle"
        app:showAsAction="always"
        />


</menu>